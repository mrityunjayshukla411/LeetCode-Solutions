#include <bits/stdc++.h>

using namespace std;

vector<int> findBall(vector<vector<int>> &grid)
{
    vector<int> res;
    int n = grid.size();
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            if(j == 0)
            {
                
            }
        }
        
    }
    
}

int main()
{

    return 0;
}